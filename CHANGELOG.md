# Changelog

本项目所有重要变更均记录在此文件中，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added
- 宜搭表单类型自动探测（`form_type.py`）：普通表单 / 流程表单，优先级 config 显式配置 > 本地缓存（30 分钟 TTL）> 接口探测（表单列表 formType 字段 / 流程接口探活），失败回落普通表单保持兼容；权限不足（Forbidden）不误判。
- 流程表单数据写入支持（04）：创建走 `processes/instances/start`，更新走 `PUT /v1.0/yida/processes/instances`，更新实例定位复用 02c 生成的 didToInst 索引；`--form-type normal|process|auto` 可强制指定类型。
- 02c 支持流程表单存量扫描（`processes/instances` 列表接口），输出结构与普通表单一致，下游 02d/04 无需改动。
- 前端表单详情展示「普通表单 / 流程表单」类型徽标（探测来源悬停可见）。
- 凭证可在部署后通过网页「设置 → 凭证」页配置（`GET/POST /api/credentials`），支持只写不回显、留空保持原值、显式置空（`clear`）、占位符拒绝与环境变量优先；非本机部署自动进入脱敏模式（掩码展示，`?view=full` 拒绝）。
- 新增「测试连接」接口（`POST /api/credentials/test`），一键校验钉钉 / 宜搭 / 轻流三端连通性。
- 轻流应用包列表拉取（`GET /api/qingflow/apps`）与宜搭表单列表拉取（`GET /api/yida/forms`），表单弹窗支持从轻流/宜搭下拉选择并自动回填 `AppKey` / `formUuid`。
- 附件迁移独立阶段（03b）完整实现：轻流 24h 签名 URL → 下载缓存 → 自有 VPS 静态托管 → 宜搭附件数组直写，主管线跳过附件字段避免重复写入。
- 子表单（TableField）转换支持：02b 从样本行提取子字段并生成子组件映射行，03 将轻流 tableValues 行数组转为宜搭对象数组，子表单内关联组件复用 didToInst 解析。
- 迁移预检新增「宜搭手动调整工作清单」：按 检查/新增/补建/补充/配置 五类动作列出需在宜搭设计器人工处理的事项，支持本地保存 MD 与一键复制。
- 文档契约：明确宜搭表单须在宜搭设计器中手动创建（宜搭无创建表单定义 API），字段标题（label）须与轻流字段名一致；新增 `docs/宜搭表单准备说明.md`、`docs/字段转换总览.md`、`docs/地址字段转换规则.md`、`docs/关联组件转换规则.md`。

### Changed
- 02b 重名字段匹配：轻流同标题多字段时优先复用旧映射（保护手工核对），无旧映射按清单顺序衔接并写入备注提示全部同名 queId，不再"只保留最后一个"。
- 预检重名字段提示：子表单内与顶层同名字段视为正常现象不计入；顶层重名仅提示不阻断，并按 mapping 实际承接情况给出未承接 queId 及其数据影响。
- 轻流字段清单（01）现收集子表单子字段（标注 `parentQueId`），顶层字段匹配跳过子字段，消除子表单字段校验误报。
- 新增轻流数据拉取开关：`run_all.py --skip-fetch` / `--force-fetch`；Web 控制台「准备数据」区新增「跳过轻流拉取」「强制全量重拉」复选框，两者互斥。
- 行政区字段标题别名匹配：宜搭「省/自治区/直辖市」「市/自治州」与轻流「省」「市」标题不一致时，通过 config `labelAliases`（含内置兜底）自动关联，避免手工补 mapping。
- 写入数据后不再自动刷新迁移预检：预检作为迁移前诊断只在准备/选择表单时执行，避免写入后立即重跑覆盖警告展示。
- 宜搭表单修改自动识别（结构指纹）：02b 生成映射时记录宜搭结构指纹，宜搭新增/删除字段后自动重映射，保留旧映射关系（含手工编辑）。
- 宜搭结构本地缓存：`02_fetch_yida_schema.py` 拉取后写本地元数据缓存（30 分钟 TTL），`--force` 强制刷新。
- 拉取数据与格式化解耦：`/api/prepare` 支持 `mode`（`all` 完整准备 / `fetch` 仅拉取+对账 / `transform` 仅字段对齐+格式化）；前端提供「拉取数据」「格式化数据」两个独立按钮。
- 实测确认宜搭流水号（SerialNumberField）不支持 API 传入值，映射表标记 `skip`，主管线不处理。
- 修正附件字段过时的"二期处理"标注，附件迁移以独立阶段为最终实现方式。

### Planned
- 支持更多轻流字段类型的自动映射。
- 迁移任务的断点续跑与失败重试可视化。
- 增量同步模式（定时拉取新增/变更记录）。

## [0.1.0] - 2026-08-03

### Added
- 首次开源发布。
- 轻流 -> 宜搭四阶段数据迁移管线：拉取、对账、格式化、写入。
- 附件迁移：轻流附件下载缓存 -> 自有 VPS 静态托管 -> 宜搭公网 URL 直写。
- 本地 Web 控制台（Flask），支持表单中心式操作与任务状态查看。
- 增量迁移：按台账跳过已迁移记录，支持已迁移/待迁移统计。
- 写入限流与任务级并发控制，规避对官方 API 的请求冲击。
- 环境变量覆盖机制（`QINGFLOW_ACCESS_TOKEN` / `DINGTALK_APP_KEY` / `DINGTALK_APP_SECRET` / `YIDA_SYSTEM_TOKEN` / `YIDA_VPS_UPLOAD_TOKEN`），凭证默认本地化，不硬编码、不中转。
